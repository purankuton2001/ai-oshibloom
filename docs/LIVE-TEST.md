# fal live smoke test — 2026-09-05

The user explicitly supplied a fal key and authorized a live test. The key is stored only in the local `.env`, permission 0600; it is excluded from the source archive. No credentials are recorded here.

- Initial anime face: successful, 5.929 seconds end to end. Not adopted because the generated appearance was too youthful for this project’s adult-only design.
- New photoreal test persona: **Yui Live**, an original adult; one face plus two derived reference images generated successfully and visually reviewed.
- Preset synthetic TTS: successful; MP3 duration 6.432 seconds.
- H3 Max reference-to-video: one idle clip and one reaction clip, each H.264 832 × 480, video duration 5.167 seconds with an AAC audio track.
- Reaction request: “Smile and wave hello to everyone.” A sampled output frame visibly shows a smile and raised waving hand.
- Comment received → downloaded reaction ready: **16.814 seconds** on this single trial. This exceeds the 15-second target. This number is not an OBS playback latency measurement.
- In-app budget estimate across all calls: **$1.08**; actual provider charges were not independently checked.
- Browser verification: generated idle video was loaded (`readyState=4`), visible and playing in the studio’s alternating video layer. The test session was stopped afterward; idle playback remains available.
- OpenAI was not configured, so the response text remains the demo preset. TTS/video generation used fal, but speech content, same-voice identity, lip synchronization and multilingual output have not been assessed by listening.
- All 11 automated tests passed after adding a one-face smoke-test option. The production build passed.

Artifacts remain in `data/personas/b76f1964/`. The Clip library contains the reaction during the current server lifetime; the actual media files and metrics persist. No YouTube broadcast was started.

## Recorded browser-driven anime test

Following the user's request, the initial anime image was reselected. The attached PNG and the in-app generated reference have identical decoded pixel MD5 (`870eef86d20440bf0fc97996366f6160`); no new personal photograph was introduced. The user expressly chose that original character for this test.

Two reference views and one idle clip were generated. A continuous timestamped capture of the actual studio browser then recorded the Start button, text entry, Send button, generation wait, visible response and return to idle. The recording is made from 100 real browser captures at their original timestamps, encoded at 30 fps with duplicated frames. It is not sped up and contains no fabricated UI states. The studio preview is muted, so the screen recording is silent.

- Entered comment: 手を振って、笑顔で挨拶して！
- Author: clover_live
- Job: 7c53b683-ba89-401d-a8f1-09e13eeb29c6
- Comment selected: 2026-09-05T11:17:28.379Z
- Video ready: 2026-09-05T11:17:38.058Z (9.682 seconds since receipt)
- Browser playback acknowledged: 2026-09-05T11:17:38.079Z (9.703 seconds since receipt)
- Playback ended: 2026-09-05T11:17:43.317Z
- Actual visible playback and return to idle were also verified by read-only browser video state.
- The reaction retains the brown bob, large eyes, cream cardigan and clover accessory, with a smiling/waving action. This is a visual spot-check, not a guarantee of exact identity in every generated frame.
- Screen recording: approximately 21.77 seconds, 1280 × 720, no speed-up, no audio.
- Additional estimated generation charges for this test: $1.00. Cumulative estimate: $2.08.
- The reply text is still a Japanese demo preset; the action video was genuinely generated by fal after the browser Send click.

`?record=1` selects a compact studio layout so the stage and chat input appear together in a normal 1280 × 720 capture. The test session was stopped afterward.

## H3 Max Turbo native audio — 2026-09-05

Added selectable `videoMode` (`reference` or `turbo`) and a sound button in the preview. Turbo uses `minimax/h3-max-turbo/image-to-video`, the selected face as its first frame, 480P and 5 seconds. Speech and mouth motion are generated in the same video, with a fixed Japanese cute adult idol voice description. Turbo's published schema has no audio reference input, so this is voice prompting, not a fixed voice identity. The standard reference route retains audio reference support. Voice Design was attempted but returned 403; no custom voice was adopted.

Actual browser comment-to-playback measurements were 7,485 ms and 6,596 ms, compared with the earlier standard reference route's 9,703 ms (small, uncontrolled sample). The successful second clip contains H.264 video and non-silent AAC audio, and browser media state confirmed muted=false during playback. Listen to the delivered result to assess Japanese pronunciation and voice preference. Three-second output is unsupported by the checked schema (minimum 5 seconds).

The engine now generates the next accepted comment while the current clip plays, buffering at most one completed clip. This overlaps generation and playback; it does not stream incomplete clips or guarantee uninterrupted playback. Tests cover ordering, cancellation and prefetched generation failure without interrupting current playback.

`turbo-live-recording.mp4` reconstructs actual browser captures using screenshot file-save timestamps. Native generated audio is aligned to observed browser playback time; this is not system audio capture. No speed-up. The last additional request failed with `User is locked. Reason: Exhausted balance`; generation was stopped. The app's reserved spending estimates remain conservative and are not the fal account balance.

## Follow-up after API key replacement — 2026-09-05

New key successfully restored generation; the key remains only in the excluded .env. Browser native-audio Turbo reaction started after 6,000 ms.

Two-comment live test: first playback at 8,598 ms after its comment. The second comment was submitted during first playback and its video began after 5,490 ms. First ended 12:54:05.337Z, second began 12:54:05.605Z: 268 ms gap. Real captures and source audio aligned to observed media time are delivered as continuous-live-recording.mp4; no speed-up, not a system-audio recording. This is one successful pair, not a latency guarantee. Replies still use the configured mock response template, not semantic LLM conversation.

Voice Design now succeeded. The persona stores a persistent synthetic voice ID and a 7.036-second Japanese reference MP3. The standard H3 Max reference route used it together with the three identity images and produced an audio-video clip with playback at 14,810 ms. Audio track is non-silent. Exact speaker similarity has not been established by listening or speaker-embedding evaluation. Turbo has no reference-audio input and continues to use a fixed voice description. Restored videoMode=turbo, session stopped. No external posting or streaming.

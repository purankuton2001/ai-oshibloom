const $=id=>document.getElementById(id);
const ja=new URLSearchParams(location.search).get('lang')==='ja';
const t=(en,jp)=>ja?jp:en;
if(new URLSearchParams(location.search).has('record'))document.body.classList.add('recording');
const translations={fixedSeed:'推し専用の固定seed',voiceDescription:'声の説明 · 毎回同じ文章を使用',voiceConsistencyNote:'推しごとに保存します。seedと説明文を固定して声の一貫性を狙いますが、完全一致を保証するものではありません。',workspace:'わたしの小さな世界',localWorkspace:'ローカルワークスペース',create:'つくる・つながる',studio:'ライブスタジオ',persona:'わたしの推し',library:'クリップライブラリ',settings:'配信設定',ownOC:'自分だけのオリジナル。',ownOCText:'小さな個性から、<br>新しい可能性へ。',localOnly:'ローカル · オープンソース · BYOK',eyebrow:'あなただけのバーチャルアイドルを、生成する。',openOBS:'OBSオーバーレイを開く',start:'セッション開始',pause:'反応を一時停止',copyOBS:'OBSリンクをコピー ↗',sessionBudget:'セッション予算',reactions:'リアクション',thisSession:'今回のセッション',onlyWhen:'コメントから、小さな瞬間を',responseTime:'直近の応答時間',target:'目標：15秒以内',upNext:'次の出番',waiting:'小さな魔法を待っています',waitingText:'選ばれたコメントがここに並びます。',demoNote:'無料デモでは、返事とキューを試せます。ステージはサンプル静止画です。実際の動きを生成するにはfalを接続してください。',connect:'プロバイダーを接続 ↗',chat:'コミュニティチャット',chatIntro:'ひとりの推し、いろんな言葉。<br>ひとこと、話しかけてみて。',chatEmpty:'ここから会話がはじまります。<br>下のプロンプトを試してください。',wave:'手を振って',dance:'少し踊って',as:'名前',protected:'オリジナルキャラ限定 · ガード有効',originalAdult:'オリジナルキャラクター · 成人',activeIdol:'選択中の推し',newIdol:'＋ 新しい推しをつくる',identity:'01 / 自分だけの個性',adultOnly:'成人のみ',name:'名前',fanName:'ファンネーム',style:'ビジュアルスタイル',appearance:'外見 · 架空の成人、24歳以上',world:'推しのいる世界',tone:'人格・話し方',boundaries:'禁止事項・返事の指示',forbidden:'避ける話題',systemPrompt:'返事への追加指示',savePersona:'人格を保存',faceStep:'02 / 顔を召喚する',faceNote:'架空の顔を生成し、選んだ顔から全身と世界観の参照画像をつくります。写真の持ち込みはできません。',draw:'✧ 顔を4枚生成',confirmAdult:'3枚とも同じオリジナルの成人で、幼い外見ではないことを確認しました。',confirmRefs:'参照セットを確認',voiceStep:'03 / 声をつくる',voiceNote:'アニメ用の合成声を一度作り、毎回の音声付き動画で同じ声を参照します。声質の完全一致は保証されません。',sampleLine:'サンプル台詞',generateVoice:'声を生成',idleStep:'04 / 出番のあいだの時間',idleNote:'反応のあいだに再生する待機プールを生成します。新しいプールが完成するまで、現在のプールを保ちます。',clipAmount:'クリップ本数',generateIdle:'待機プールを生成',cancel:'中止',savedMoments:'集まった、小さな瞬間。',libraryNote:'顔の一貫性を1〜5で評価できます。デモ静止画は動画品質の検証には含めません。',export:'↓ 実測ログを出力',generationSettings:'生成・接続',backend:'動画プロバイダー',replyProvider:'返事プロバイダー',keyNote:'このプロジェクトの.envにキーを設定して再起動してください。キーはサーバーだけで扱います。',platform:'チャット取得元',videoId:'YouTubeの配信動画ID',replyEnabled:'コメントと同じ言語で返す',paceBudget:'ペース・予算',budget:'セッション上限（USD）',duration:'クリップの長さ（秒）',interval:'反応の間隔（秒）',cooldown:'同じ視聴者の待ち時間（秒）',maxQueue:'キューの上限',budgetNote:'画像・待機動画を含む生成費の見積額を、送信前に確保します。LLMのトークン費用は含みません。実請求はプロバイダーで確認してください。',resetBudget:'使用額の見積をリセット（停止中のみ）',guardSettings:'オリジナルを守る',approval:'生成前にすべてのコメントを承認する',prefix:'コマンド接頭辞 · 空欄なら全コメント',blockWords:'追加で除外する名前・単語 · 1行1件',guardNote:'同梱の英語・韓国語・日本語の人物・作品名は常にブロックします。リストですべての名前を検出することはできません。承認モードと別名の追加を併用してください。',obsReady:'配信のための、小さな舞台。',obsNote:'OBSのブラウザソースに1920×1080で追加します。音声は?audio=1で有効になります。配信サービスで合成コンテンツを開示してください。',badgeNote:'オーバーレイにはAI生成の表示が常に付きます。',saveSettings:'設定を保存',footer:'想像した顔と、つながるコミュニティ。'};
if(ja){document.documentElement.lang='ja';document.querySelectorAll('[data-i]').forEach(el=>{if(translations[el.dataset.i])el.innerHTML=translations[el.dataset.i];});$('comment').placeholder='小さな魔法を、ひとこと…';$('stage').src='/overlay?preview=1&lang=ja';$('language').textContent='English';}
$('language').onclick=()=>{const url=new URL(location.href);if(ja)url.searchParams.delete('lang');else url.searchParams.set('lang','ja');location.href=url;};
const pageCopy={studio:[t('A little world. All yours.','小さな世界。あなたのもの。'),t('Your idol is here. Let your community bring the moment to life.','ここにいる推しと、みんなのひとことで生まれる時間。')],persona:[t('Someone only you could imagine.','あなたにしか、想像できない推し。'),t('A face, a voice, a little personality. Make them your own.','顔も、声も、個性も。あなたらしいひとりを。')],library:[t('Keep the little moments.','小さな瞬間を、残しておく。'),t('Generated reactions, idle clips and a record of every experiment.','反応クリップと待機映像、ひとつひとつの実験記録。')],settings:[t('Your stream, your rhythm.','あなたのペースで、配信を。'),t('Connect your tools. Set the pace. Keep your world original.','道具をつなぎ、ペースを決め、オリジナルの世界を守る。')]};
let page='studio',state,ws,personaId,initialized=false,toastTimer,feedSignature='',queueSignature='',librarySignature='';
function go(next){page=pageCopy[next]?next:'studio';document.querySelectorAll('.page').forEach(el=>el.classList.toggle('hidden',el.id!==page));document.querySelectorAll('[data-page]').forEach(el=>el.classList.toggle('active',el.dataset.page===page));$('pageTitle').textContent=pageCopy[page][0];$('pageSubtitle').textContent=pageCopy[page][1];$('breadcrumb').textContent=t({studio:'Live studio',persona:'My idol',library:'Clip library',settings:'Stream settings'}[page],translations[page]);location.hash=page;}
document.querySelectorAll('[data-page]').forEach(el=>el.onclick=()=>go(el.dataset.page));document.querySelectorAll('[data-go]').forEach(el=>el.onclick=()=>go(el.dataset.go));go(location.hash.slice(1)||'studio');
const escape=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
function toast(text){$('toast').textContent=text;$('toast').classList.add('show');clearTimeout(toastTimer);toastTimer=setTimeout(()=>$('toast').classList.remove('show'),5000);}
async function api(route,body){const res=await fetch(route,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(body||{})});const data=await res.json();if(!res.ok)throw new Error(data.error||'Request failed');return data;}
function action(id,fn){$(id).onclick=async()=>{const button=$(id);button.disabled=true;try{await fn();}catch(e){toast(e.message);}finally{button.disabled=false;}};}
async function refresh(){const res=await fetch('/api/state');if(!res.ok)throw new Error('Could not connect to the studio');render(await res.json());}
function fill(form,values){for(const el of form.elements){if(!el.name||!(el.name in values))continue;const v=values[el.name];if(el.type==='checkbox')el.checked=v;else el.value=Array.isArray(v)?v.join('\n'):v;}}
function values(form){return Object.fromEntries([...form.elements].filter(el=>el.name).map(el=>[el.name,el.type==='checkbox'?el.checked:el.type==='number'?Number(el.value):el.name==='blockedWords'?el.value.split('\n').map(x=>x.trim()).filter(Boolean):el.value]));}
function render(s){state=s;const p=s.persona;
  if(!initialized){fill($('settingsForm'),s.settings);initialized=true;}
  if(personaId!==p.id){personaId=p.id;fill($('personaForm'),p);$('adultConfirm').checked=false;}
  $('start').innerHTML=s.running?`■ ${t('Stop session','セッション停止')}`:`▶ ${t('Start session','セッション開始')}`;
  $('pause').textContent=s.paused?t('Resume reactions','反応を再開'):t('Pause reactions','反応を一時停止');$('pause').disabled=!s.running;
  $('stageStatus').textContent=s.running?(s.paused?t('PAUSED','一時停止'):t('SESSION ACTIVE','セッション中')):t('STUDIO PREVIEW','スタジオプレビュー');
  $('stageName').textContent=p.name;$('stageFan').textContent=t(`A little magic for the ${p.fanName||'community'}`,`${p.fanName||'みんな'}に、小さな魔法を`);
  $('stageMode').textContent=s.settings.backend==='mock'?t('DEMO · STILL IMAGE','デモ · 静止画'):t('AI GENERATED','AI生成');
  document.querySelector('.stage-persona img').src=p.refs[0]||'/assets/yui-demo.png';
  document.querySelector('.gentle-note p').textContent=s.settings.backend==='mock'?t('You’re in the free demo. Replies and queues work; the stage uses a sample image. Connect fal to generate real motion.','無料デモです。ステージはサンプル静止画です。実際の動きを生成するにはfalを接続してください。'):t('Live generation is connected. Reactions use your provider account; your session budget applies.','実生成に接続しています。生成費用は接続したアカウントに請求され、セッション予算が適用されます。');
  $('idleStatus').textContent=p.idle.length?t(`${p.idle.length} idle clips · continuous playback`,`${p.idle.length}本の待機クリップ · 連続再生`):(p.demo?t('Demo stage is ready','デモステージの準備完了'):t('Reference preview · create an idle pool','参照画像プレビュー · 待機プールを生成できます'));
  $('budgetValue').innerHTML=`$${s.spent.toFixed(2)} <small>/ $${s.settings.budget.toFixed(2)}</small>`;$('budgetBar').style.width=Math.min(100,s.spent/Math.max(s.settings.budget,.001)*100)+'%';
  $('reactionValue').innerHTML=`${s.stats.generated} <small>${t('this session','今回のセッション')}</small>`;
  const latest=[...s.jobs].reverse().find(j=>j.latency);$('latencyValue').innerHTML=latest?`${(latest.latency/1000).toFixed(1)} <small>s</small>`:'—';
  $('chatSource').textContent=s.settings.platform==='youtube'?'YOUTUBE LIVE':t('PLAYGROUND','プレイグラウンド');
  $('busyBanner').hidden=!s.busy;$('busyBanner').textContent=t('Creating: ','生成中：')+s.busy;
  const jobs=s.jobs.filter(j=>['approval','queued','generating','ready','playing'].includes(j.status));$('queueCount').textContent=t(`${jobs.length} in queue`,`${jobs.length}件`);
  const sig=JSON.stringify(jobs);if(sig!==queueSignature){queueSignature=sig;renderQueue(jobs);}
  const feed=s.jobs.filter(j=>j.status!=='rejected');const feedSig=JSON.stringify(feed.map(j=>[j.id,j.reply,j.status,j.playbackStartedAt]));if(feedSig!==feedSignature){feedSignature=feedSig;renderFeed(feed);}
  const clips=[...p.idle.map(c=>({clip:c,text:t('An idle moment','待機中のひととき'),id:c.id})),...s.jobs.filter(j=>j.clip)];$('clipCount').textContent=clips.length;
  const ls=JSON.stringify(clips);if(ls!==librarySignature){librarySignature=ls;renderLibrary(clips);}
  $('personaName').textContent=p.name;$('personaDescription').textContent=p.fanName?`${p.fanName} ✳`:'';$('personaHero').src=p.refs[0]||'/assets/yui-demo.png';
  const ps=$('personaSelect');if(JSON.stringify(s.personas)!==ps.dataset.signature){ps.innerHTML=s.personas.map(p=>`<option value="${escape(p.id)}">${escape(p.name)}</option>`).join('');ps.dataset.signature=JSON.stringify(s.personas);}ps.value=p.id;
  $('refsStatus').textContent=`${p.refs.length} / 3 · ${p.confirmed?t('CONFIRMED','確認済み'):t('UNCONFIRMED','未確認')}`;
  const refs=$('references');if(refs.dataset.signature!==JSON.stringify(p.refs)){refs.dataset.signature=JSON.stringify(p.refs);refs.innerHTML=p.refs.map((url,i)=>`<figure><img src="${escape(url)}" alt="${['Face','Full body','Scene'][i]} reference"><figcaption>${t(['Face','Full body','Scene'][i],['顔','全身','世界観'][i])}${p.demo?' · DEMO':''}</figcaption></figure>`).join('');}
  const candidates=$('candidates');if(candidates.dataset.signature!==JSON.stringify(s.candidates)){candidates.dataset.signature=JSON.stringify(s.candidates);candidates.innerHTML=s.candidates.map((c,i)=>`<button data-adopt="${c.id}"><img src="${escape(c.url)}" alt="Candidate ${i+1}">${c.demo?t('Demo sample · not newly generated','デモサンプル · 新規生成ではありません'):t('Choose this face','この顔を選ぶ')}</button>`).join('');candidates.querySelectorAll('button').forEach(b=>b.onclick=()=>create('adopt',{id:b.dataset.adopt}));}
  $('voicePlayer').hidden=!p.voice;if(p.voice&&$('voicePlayer').getAttribute('src')!==p.voice)$('voicePlayer').src=p.voice;
  $('idleProgress').textContent=s.busy.startsWith('idle')?s.busy:t(`${p.idle.length} clips in the current pool`,`${p.idle.length}本の待機クリップを保存済み`);
  $('providerStatus').textContent=`fal ${s.capabilities.fal?'✓':'—'} · OpenAI ${s.capabilities.openai?'✓':'—'} · YouTube ${s.capabilities.youtube?'✓':'—'} · ComfyUI ${s.capabilities.local?'✓':'—'}`;
  $('obsUrl').textContent=obsURL();
}
function renderQueue(jobs){$('queue').innerHTML=jobs.length?jobs.map(j=>`<div class="job"><span class="pill">${escape(t(j.status,({approval:'承認待ち',queued:'待機',generating:'生成中',ready:'再生待ち',playing:'再生中'})[j.status]))}</span><div class="job-text"><strong>${escape(j.author)}</strong><p>${escape(j.text)}</p></div>${j.status==='approval'?`<button data-approve="${j.id}">${t('Approve','承認')}</button>`:''}${['approval','queued'].includes(j.status)?`<button data-reject="${j.id}" aria-label="Reject">×</button>`:''}</div>`).join(''):`<div class="empty"><span>✧</span><div><strong>${t('Room for a little magic','小さな魔法を待っています')}</strong><p>${t('Accepted comments will appear here, ready for their moment.','選ばれたコメントがここに並びます。')}</p></div></div>`;$('queue').querySelectorAll('[data-approve],[data-reject]').forEach(b=>b.onclick=()=>api('/api/approve',{id:b.dataset.approve||b.dataset.reject,accept:!!b.dataset.approve}).catch(e=>toast(e.message)));}
function renderFeed(jobs){if(!jobs.length)return;const feed=$('chatFeed');feed.innerHTML=jobs.map(j=>`<div class="chat-message"><span class="chat-avatar">${escape(j.author.slice(0,1).toUpperCase())}</span><div><strong>${escape(j.author)}</strong><time>${new Date(j.receivedAt).toLocaleTimeString([],{hour:'2-digit',minute:'2-digit'})}</time><p>${escape(j.text)}</p>${j.reply&&j.playbackStartedAt?`<p class="chat-reply">✳ ${escape(j.reply)}</p>`:''}${j.error?`<p>${escape(j.error)}</p>`:''}</div></div>`).join('');feed.scrollTop=feed.scrollHeight;}
function renderLibrary(clips){$('clipLibrary').innerHTML=clips.length?clips.map(j=>`<article class="clip-card">${j.clip.kind==='video'?`<video src="${escape(j.clip.url)}" controls preload="metadata"></video>`:`<img src="${escape(j.clip.url)}" alt="Demo still image">`}<div><h3>${escape(j.text)}</h3><p>${j.clip.demo?t('Demo still · no generated motion','デモ静止画 · 動画ではありません'):t('Generated video','生成動画')} · ${j.clip.duration}s</p>${'status'in j?`<div class="rating" aria-label="Face consistency">${[1,2,3,4,5].map(n=>`<button class="${j.rating===n?'selected':''}" data-rate="${j.id}" data-score="${n}" aria-label="${n} out of 5">${n} ★</button>`).join('')}</div>`:''}</div></article>`).join(''):`<div class="empty"><span>✧</span><p>${t('Your first moment is still ahead. Send a comment in the studio.','最初のひとときは、これから。スタジオでコメントを送ってみてください。')}</p></div>`;$('clipLibrary').querySelectorAll('[data-rate]').forEach(b=>b.onclick=()=>api('/api/rate',{id:b.dataset.rate,score:Number(b.dataset.score)}).catch(e=>toast(e.message)));}
function obsURL(){return location.origin+'/overlay'+(ja?'?lang=ja':'');}
action('obs',()=>{window.open(obsURL(),'ai-oshibloom-overlay');});action('copyOBS',async()=>{await navigator.clipboard.writeText(obsURL());toast(t('OBS link copied. Use a 1920 × 1080 Browser Source.','OBSリンクをコピーしました。1920×1080のブラウザソースで使えます。'));});
action('start',()=>api('/api/control',{action:state.running?'stop':'start'}));action('pause',()=>api('/api/control',{action:state.paused?'resume':'pause'}));
action('resetBudget',async()=>{if(confirm(t('Reset the spent estimate? This does not refund provider charges.','使用額の見積をリセットしますか？プロバイダーへの支払いは取り消されません。')))await api('/api/control',{action:'reset-budget'});});
$('commentForm').onsubmit=async e=>{e.preventDefault();if(!state.running){toast(t('Start the session, then send your first comment.','セッションを開始してからコメントを送ってください。'));return;}try{const r=await api('/api/comment',{text:$('comment').value,author:$('author').value});if(r.accepted){$('comment').value='';}else toast(t('Comment skipped: ','コメントをスキップ：')+r.reason);}catch(e){toast(e.message);}};
document.querySelectorAll('[data-comment]').forEach(b=>b.onclick=()=>{$('comment').value=b.dataset.comment;$('comment').focus();});
$('settingsForm').onsubmit=async e=>{e.preventDefault();try{await api('/api/settings',values(e.target));toast(t('Settings saved.','設定を保存しました。'));}catch(e){toast(e.message);}};
$('personaForm').onsubmit=async e=>{e.preventDefault();try{const p=await api('/api/persona/save',values(e.target));fill(e.target,p);toast(t('Personality saved.','人格を保存しました。'));}catch(e){toast(e.message);}};
action('newPersona',async()=>{const name=prompt(t('A name for your original idol?','新しいオリジナルの推しの名前は？'),'Nova');if(name)await api('/api/persona/create',{name});});
$('personaSelect').onchange=async()=>{try{await api('/api/persona/select',{id:$('personaSelect').value});}catch(e){toast(e.message);$('personaSelect').value=state.persona.id;}};
async function create(kind,payload={}){try{await api('/api/create/'+kind,payload);toast(t('Ready. Your idol is one step closer.','完了しました。推しの準備が一歩進みました。'));}catch(e){toast(e.message);}}
action('faces',async()=>{await api('/api/persona/save',values($('personaForm')));await create('faces');});
action('confirmRefs',()=>create('confirm',{adult:$('adultConfirm').checked}));action('voice',()=>create('voice',{text:$('voiceLine').value}));
action('generateIdle',async()=>{const count=Number($('idleCount').value);const est=state.settings.backend==='fal'?count*(state.settings.duration*.08+state.persona.refs.length*.02):0;if(confirm(t(`Create ${count} idle clips? Baseline estimate $${est.toFixed(2)}. Your configured prices may differ.`,`待機クリップを${count}本生成しますか？標準見積は$${est.toFixed(2)}です。設定した単価によって変わります。`)))await create('idle',{count});});action('cancelIdle',()=>api('/api/create/cancel'));
function connect(){ws=new WebSocket((location.protocol==='https:'?'wss://':'ws://')+location.host+'/ws');ws.onopen=()=>{$('connection').textContent=t('Connected locally','ローカル接続中');};ws.onmessage=e=>{const m=JSON.parse(e.data);if(m.type==='state')render(m.state);};ws.onclose=()=>{$('connection').textContent=t('Reconnecting…','再接続中…');setTimeout(connect,1500);};}
refresh().catch(e=>toast(e.message));connect();

$('modeDirector').onclick=async()=>{try{await api('/api/control',{action:'stop'});location.href='/director.html';}catch(e){alert(e.message);}};
$('modeTurbo').onclick=async()=>{try{await api('/api/control',{action:'stop'});await api('/api/settings',{backend:'fal',videoMode:'turbo'});}catch(e){alert(e.message);}};
